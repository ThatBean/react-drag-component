:root {
  --indent-height: 40px;
  --indent-left: 10px;
  --indent-right: 10px;
  --link-size: 1px;
  --link-color: #ddd;
  --indent-root: 12px;
}

:local(.tab) {
  cursor: default;
  display: flex;
  flex-direction: row;
  align-items: center;
  /*justify-content: space-between;*/
  height: var(--indent-height);
  padding-right: var(--indent-root);
  &:hover { background: rgba(0, 0, 0, 0.05); }
  &:hover .edit-button { opacity: 0.4; }
  &.root {
    height: calc(var(--indent-height) * 1.2);
    border-top: 1px solid #EAEBED;
    padding: 0 var(--indent-root);
  }
  &.select { color: #f75b50; }
  &.hover-source { background-color: rgba(247, 91, 80, 0.1); }
  &.hover-preview { background-color: #EAEBED; }

  & .icon {
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    &.leaf { font-size: 16px; }
  }
  & .tab-name { flex: 1 1; }
  & .edit-button {
    cursor: pointer;
    font-size: 16px;
    padding: 0 2px;
    opacity: 0;
    &:hover { opacity: 1; }
  }
}
